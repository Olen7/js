/*
Напишіть функцію, яка отримує як аргумент радіус кола,
 а повертає його площу.
 Викликайте її через 2 секунди після завантаження сторінки.
*/
setTimeout(() => calcCircle(10), 2000)
function calcCircle(radius){ //радіус в квадраті помножений на 2
   return console.log((radius ** 2) * Math.PI);
}
